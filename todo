import - offset, scale, rotation
fill geometry persists
parse sparseCells -> cells

hierarchical
    - gik dimensions
    - origin
    - rotation
    - load composite materials
    - change bounding box
    - placement ui 3D

rendering callbacks
